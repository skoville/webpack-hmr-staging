import * as webpack from 'webpack';
import * as fs from 'fs';

// Recursive read of the './source' directory.
// Each file is its own webpack bundle, which will be output to the dist folder.

module.exports = [
    {

    }
];